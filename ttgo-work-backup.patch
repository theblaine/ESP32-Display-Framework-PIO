diff --git a/lib/Display_Boards/DisplayConfig.h b/lib/Display_Boards/DisplayConfig.h
index a98bdb5..0aa3c07 100644
--- a/lib/Display_Boards/DisplayConfig.h
+++ b/lib/Display_Boards/DisplayConfig.h
@@ -5,12 +5,18 @@
 
 #pragma once
 
+#if defined(DISPLAY_BOARD_WAVESHARE_147)
+
 #include "Waveshare_ESP32S3_LCD_147/WaveshareDisplayConfig.h"
+namespace DisplayConfig = WaveshareDisplayConfig;
 
-/**
- * @brief Board-neutral name for the active display configuration.
- *
- * This alias allows the graphics and application layers to consume display
- * geometry without depending directly on a display-controller driver.
- */
-namespace DisplayConfig = WaveshareDisplayConfig;
\ No newline at end of file
+#elif defined(DISPLAY_BOARD_TTGO_T_DISPLAY)
+
+#include "TTGO_T_Display/TTGODisplayConfig.h"
+namespace DisplayConfig = TTGODisplayConfig;
+
+#else
+
+#error "No display board selected. Define DISPLAY_BOARD_WAVESHARE_147 or DISPLAY_BOARD_TTGO_T_DISPLAY."
+
+#endif
\ No newline at end of file
diff --git a/lib/Display_Boards/Waveshare_ESP32S3_LCD_147/WaveshareDisplayConfig.h b/lib/Display_Boards/Waveshare_ESP32S3_LCD_147/WaveshareDisplayConfig.h
index b4cf4d8..662b6ed 100644
--- a/lib/Display_Boards/Waveshare_ESP32S3_LCD_147/WaveshareDisplayConfig.h
+++ b/lib/Display_Boards/Waveshare_ESP32S3_LCD_147/WaveshareDisplayConfig.h
@@ -16,7 +16,7 @@ namespace WaveshareDisplayConfig
     inline constexpr uint16_t Height = 320;
 
     // SPI bus configuration.
-    inline constexpr uint32_t SpiFrequency = 80000000;
+    inline constexpr uint32_t SpiFrequency = 40000000;
     inline constexpr int8_t PinMiso = -1;
     inline constexpr int8_t PinMosi = 45;
     inline constexpr int8_t PinClock = 40;
diff --git a/lib/Display_ST7789/Display_ST7789.cpp b/lib/Display_ST7789/Display_ST7789.cpp
index a51d05e..971d2fb 100644
--- a/lib/Display_ST7789/Display_ST7789.cpp
+++ b/lib/Display_ST7789/Display_ST7789.cpp
@@ -11,14 +11,19 @@ namespace
 
     uint8_t CurrentBrightness = 100;
 
-    SPIClass LCDspi(FSPI);
+#if defined(CONFIG_IDF_TARGET_ESP32S3)
+    SPIClass &LCDspi = SPI;
+#else
+    SPIClass LCDspi(VSPI);
+#endif
 
     void SPI_Init()
     {
         LCDspi.begin(
             DisplayConfig::PinClock,
             DisplayConfig::PinMiso,
-            DisplayConfig::PinMosi);
+            DisplayConfig::PinMosi,
+            DisplayConfig::PinChipSelect);
     }
 
     void LCD_WriteCommand(uint8_t command)
@@ -49,6 +54,12 @@ namespace
         LCDspi.endTransaction();
     }
 
+    void LCD_WriteAddress(uint16_t address)
+    {
+        LCD_WriteData(static_cast<uint8_t>(address >> 8));
+        LCD_WriteData(static_cast<uint8_t>(address & 0xFF));
+    }
+
     void LCD_WriteDataWord(uint16_t data)
     {
         LCDspi.beginTransaction(
@@ -63,23 +74,42 @@ namespace
         LCDspi.endTransaction();
     }
 
-    void LCD_WriteDataBytes(
-        uint8_t* writeData,
-        uint8_t* readData,
-        uint32_t size)
+void LCD_WriteDataBytes(
+    uint8_t *writeData,
+    uint32_t size)
+{
+    constexpr uint32_t ChunkSize = 4096;
+    static uint8_t receiveBuffer[ChunkSize];
+
+    LCDspi.beginTransaction(
+        SPISettings(DisplayConfig::SpiFrequency, MSBFIRST, SPI_MODE0));
+
+    digitalWrite(DisplayConfig::PinChipSelect, LOW);
+    digitalWrite(DisplayConfig::PinDataCommand, HIGH);
+
+    uint32_t offset = 0;
+
+    while (offset < size)
     {
-        LCDspi.beginTransaction(
-            SPISettings(DisplayConfig::SpiFrequency, MSBFIRST, SPI_MODE0));
+        const uint32_t bytesRemaining = size - offset;
 
-        digitalWrite(DisplayConfig::PinChipSelect, LOW);
-        digitalWrite(DisplayConfig::PinDataCommand, HIGH);
+        const uint32_t bytesToTransfer =
+            bytesRemaining > ChunkSize
+                ? ChunkSize
+                : bytesRemaining;
 
-        LCDspi.transferBytes(writeData, readData, size);
+        LCDspi.transferBytes(
+            writeData + offset,
+            receiveBuffer,
+            bytesToTransfer);
 
-        digitalWrite(DisplayConfig::PinChipSelect, HIGH);
-        LCDspi.endTransaction();
+        offset += bytesToTransfer;
     }
 
+    digitalWrite(DisplayConfig::PinChipSelect, HIGH);
+    LCDspi.endTransaction();
+}
+
     void LCD_Reset()
     {
         digitalWrite(DisplayConfig::PinChipSelect, LOW);
@@ -109,14 +139,52 @@ void LCD_Init(void)
     pinMode(DisplayConfig::PinDataCommand, OUTPUT);
     pinMode(DisplayConfig::PinReset, OUTPUT);
 
+    digitalWrite(DisplayConfig::PinChipSelect, HIGH);
+    digitalWrite(DisplayConfig::PinDataCommand, HIGH);
+    digitalWrite(DisplayConfig::PinReset, HIGH);
+
     Backlight_Init();
     SPI_Init();
     LCD_Reset();
+#if defined(DISPLAY_BOARD_TTGO_T_DISPLAY)
+
+    // Software reset.
+    LCD_WriteCommand(0x01);
+    delay(150);
+
+    // Exit sleep mode.
+    LCD_WriteCommand(0x11);
+    delay(120);
+
+    // 16-bit RGB565 color mode.
+    LCD_WriteCommand(0x3A);
+    LCD_WriteData(0x55);
+    delay(10);
 
-    // Start initialization sequence.
+    // Memory access control.
+    LCD_WriteCommand(0x36);
+    LCD_WriteData(0x00);
+
+    // Normal display mode.
+    LCD_WriteCommand(0x13);
+    delay(10);
+
+    // Display inversion on.
+    LCD_WriteCommand(0x21);
+    delay(10);
+
+    // Display on.
+    LCD_WriteCommand(0x29);
+    delay(120);
+
+    return;
+
+#endif
+    // Sleep out.
     LCD_WriteCommand(0x11);
     delay(120);
 
+    // Memory access control.
     LCD_WriteCommand(0x36);
 
     if (DisplayConfig::Horizontal)
@@ -128,6 +196,7 @@ void LCD_Init(void)
         LCD_WriteData(0x70);
     }
 
+    // 16-bit RGB565 color mode.
     LCD_WriteCommand(0x3A);
     LCD_WriteData(0x05);
 
@@ -202,12 +271,12 @@ void LCD_Init(void)
     LCD_WriteData(0x29);
     LCD_WriteData(0x32);
 
+    // Display inversion on.
     LCD_WriteCommand(0x21);
 
-    LCD_WriteCommand(0x11);
-    delay(120);
-
+    // Display on.
     LCD_WriteCommand(0x29);
+    delay(20);
 }
 
 void LCD_SetCursor(
@@ -218,31 +287,47 @@ void LCD_SetCursor(
 {
     if (DisplayConfig::Horizontal)
     {
+        const uint16_t columnStart =
+            xStart + DisplayConfig::OffsetX;
+
+        const uint16_t columnEnd =
+            xEnd + DisplayConfig::OffsetX;
+
+        const uint16_t rowStart =
+            yStart + DisplayConfig::OffsetY;
+
+        const uint16_t rowEnd =
+            yEnd + DisplayConfig::OffsetY;
+
         LCD_WriteCommand(0x2A);
-        LCD_WriteData(xStart >> 8);
-        LCD_WriteData(xStart + DisplayConfig::OffsetX);
-        LCD_WriteData(xEnd >> 8);
-        LCD_WriteData(xEnd + DisplayConfig::OffsetX);
+        LCD_WriteAddress(columnStart);
+        LCD_WriteAddress(columnEnd);
 
         LCD_WriteCommand(0x2B);
-        LCD_WriteData(yStart >> 8);
-        LCD_WriteData(yStart + DisplayConfig::OffsetY);
-        LCD_WriteData(yEnd >> 8);
-        LCD_WriteData(yEnd + DisplayConfig::OffsetY);
+        LCD_WriteAddress(rowStart);
+        LCD_WriteAddress(rowEnd);
     }
     else
     {
+        const uint16_t columnStart =
+            yStart + DisplayConfig::OffsetY;
+
+        const uint16_t columnEnd =
+            yEnd + DisplayConfig::OffsetY;
+
+        const uint16_t rowStart =
+            xStart + DisplayConfig::OffsetX;
+
+        const uint16_t rowEnd =
+            xEnd + DisplayConfig::OffsetX;
+
         LCD_WriteCommand(0x2A);
-        LCD_WriteData(yStart >> 8);
-        LCD_WriteData(yStart + DisplayConfig::OffsetY);
-        LCD_WriteData(yEnd >> 8);
-        LCD_WriteData(yEnd + DisplayConfig::OffsetY);
+        LCD_WriteAddress(columnStart);
+        LCD_WriteAddress(columnEnd);
 
         LCD_WriteCommand(0x2B);
-        LCD_WriteData(xStart >> 8);
-        LCD_WriteData(xStart + DisplayConfig::OffsetX);
-        LCD_WriteData(xEnd >> 8);
-        LCD_WriteData(xEnd + DisplayConfig::OffsetX);
+        LCD_WriteAddress(rowStart);
+        LCD_WriteAddress(rowEnd);
     }
 
     LCD_WriteCommand(0x2C);
@@ -253,23 +338,21 @@ void LCD_addWindow(
     uint16_t yStart,
     uint16_t xEnd,
     uint16_t yEnd,
-    uint16_t* color)
+    uint16_t *color)
 {
     const uint16_t width = xEnd - xStart + 1;
     const uint16_t height = yEnd - yStart + 1;
+
     const uint32_t numberOfBytes =
         static_cast<uint32_t>(width) *
         static_cast<uint32_t>(height) *
         sizeof(uint16_t);
 
-    uint8_t readData[numberOfBytes];
-
     LCD_SetCursor(xStart, yStart, xEnd, yEnd);
 
-    LCD_WriteDataBytes(
-        reinterpret_cast<uint8_t*>(color),
-        readData,
-        numberOfBytes);
+LCD_WriteDataBytes(
+    reinterpret_cast<uint8_t*>(color),
+    numberOfBytes);
 }
 
 void Display_SetBrightness(uint8_t percent)
diff --git a/platformio.ini b/platformio.ini
index c05927d..c1010c2 100644
--- a/platformio.ini
+++ b/platformio.ini
@@ -1,51 +1,93 @@
 [platformio]
 default_envs = demo_display_text
 
+
+; ============================================================
+; Shared settings
+; ============================================================
+
 [env]
 platform = https://github.com/pioarduino/platform-espressif32/releases/download/stable/platform-espressif32.zip
-board = esp32-s3-devkitc1-n16r8
 framework = arduino
 
+monitor_speed = 115200
+
+
+; ============================================================
+; Waveshare ESP32-S3-LCD-1.47 defaults
+;
+; All existing demo environments inherit these settings.
+; ============================================================
+
+board = esp32-s3-devkitc1-n16r8
 
 board_build.flash_mode = qio
 board_build.flash_size = 16MB
 
 build_flags =
     -DBOARD_HAS_PSRAM
-
-monitor_speed = 115200
+    -DDISPLAY_BOARD_WAVESHARE_147
 
 
-; ===== Demo selection =====
+; ============================================================
+; Waveshare demo environments
+; ============================================================
 
 [env:demo_display_widgets]
 build_src_filter =
     +<demos/display_widgets/>
 
+
 [env:demo_display_console]
-build_src_filter=
+build_src_filter =
     +<demos/display_console/>
 
+
 [env:demo_display_text]
-build_src_filter=
+build_src_filter =
     +<demos/display_text/>
 
+
 [env:demo_display_graphics]
 build_src_filter =
     +<demos/display_graphics/>
 
+
 [env:demo_factory]
 build_src_filter =
     +<demos/factory/>
 
+
 [env:demo_png]
 build_src_filter =
     +<demos/png/>
 
+
 [env:demo_wifi]
 build_src_filter =
     +<demos/wifi/>
 
+
 [env:demo_rgb]
 build_src_filter =
     +<demos/rgb/>
+
+
+; ============================================================
+; TTGO T-Display V1.1 test environment
+;
+; Classic ESP32, 4 MB flash, no PSRAM.
+; Uses the same display_graphics demo as the Waveshare test.
+; ============================================================
+
+[env:ttgo_display_graphics]
+board = esp32dev
+
+board_build.flash_mode = dio
+board_build.flash_size = 4MB
+
+build_flags =
+    -DDISPLAY_BOARD_TTGO_T_DISPLAY
+
+build_src_filter =
+    +<demos/display_graphics/>
\ No newline at end of file
